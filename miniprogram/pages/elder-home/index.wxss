.container {
  min-height: 100vh;
  background: #f5f5f5;
  padding: 30rpx;
}

.header {
  background: linear-gradient(135deg, #1a237e 0%, #283593 100%);
  border-radius: 24rpx;
  padding: 40rpx 30rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 12rpx 32rpx rgba(26, 35, 126, 0.3);
}

.welcome-text {
  font-size: 60rpx;
  font-weight: bold;
  color: white;
  margin-bottom: 16rpx;
  text-align: center;
}

.date-time {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.date {
  font-size: 36rpx;
  color: rgba(255, 255, 255, 0.95);
  font-weight: 500;
}

.time {
  font-size: 54rpx;
  font-weight: bold;
  color: white;
}

.emergency-section {
  margin-bottom: 30rpx;
}

.section-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #1a237e;
  margin-bottom: 20rpx;
  padding-left: 12rpx;
  border-left: 6rpx solid #1a237e;
}

.emergency-buttons {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.emergency-btn {
  width: 100%;
  background: linear-gradient(135deg, #d32f2f 0%, #b71c1c 100%);
  border-radius: 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx 30rpx;
  border: none;
  box-shadow: 0 10rpx 32rpx rgba(211, 47, 47, 0.4);
  position: relative;
  overflow: hidden;
  min-height: 180rpx;
}

.emergency-btn::after {
  border: none;
}

.emergency-btn:active {
  transform: scale(0.98);
  box-shadow: 0 6rpx 20rpx rgba(211, 47, 47, 0.3);
}

.emergency-btn-icon {
  font-size: 72rpx;
  margin-bottom: 16rpx;
}

.emergency-btn-name {
  font-size: 40rpx;
  font-weight: bold;
  color: white;
  margin-bottom: 8rpx;
  text-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.3);
}

.emergency-btn-phone {
  font-size: 32rpx;
  color: rgba(255, 255, 255, 0.98);
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
  font-weight: 500;
}

.buttons-container {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.function-btn {
  width: 100%;
  height: 140rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  padding: 0 30rpx;
  border: none;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.1);
  position: relative;
  overflow: hidden;
  transition: all 0.2s ease;
}

.function-btn::after {
  border: none;
}

.function-btn:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
}

.weather-btn {
  background: linear-gradient(135deg, #1976d2 0%, #0d47a1 100%);
}

.bus-btn {
  background: linear-gradient(135deg, #388e3c 0%, #1b5e20 100%);
}

.community-btn {
  background: linear-gradient(135deg, #7b1fa2 0%, #4a148c 100%);
}

.contacts-btn {
  background: linear-gradient(135deg, #ff6f00 0%, #e65100 100%);
}

.settings-btn {
  background: linear-gradient(135deg, #616161 0%, #212121 100%);
}

.btn-icon {
  font-size: 64rpx;
  margin-right: 30rpx;
  width: 80rpx;
  text-align: center;
}

.btn-text {
  font-size: 36rpx;
  font-weight: bold;
  color: white;
  text-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.2);
  flex: 1;
}